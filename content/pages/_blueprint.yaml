path: /{slug}/               # The URL path format.
view: /views/base.html      # The template to use.
title: Pridoc                 # Collection's title.

localization:
  path: /{locale}/{slug}/    # Localized URL path format.